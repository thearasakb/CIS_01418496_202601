git config --global user.name "ใส่ชื่อusername"  แนะนำให้ใส่ให้ตรงกับ github

git config --global user.email "ใส่email github" แนะนำให้ใส่ให้ตรงกับ github

git clone https://github.com/yaowaphaa/CIS_01418496_202601.gitgithub.com

การอัพโหลดไฟล์
git add . //เป็นการอัพไฟล์ทั้งหมดใน vscode

git commit -m "เขียนอะไรลงไปก็ได้เหมือนเป็นการบอกว่าเราทำอะไรไป"

git push -u origin main ใช้ การอัพครั้งแรกของเครื่องคอม ครั้งต่อไปใช้ git push แค่นี้
